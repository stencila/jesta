export enum Method {
  build = 'build',
  clean = 'clean',
  compile = 'compile',
  decode = 'decode',
  encode = 'encode',
  enrich = 'enrich',
  execute = 'execute',
  pipe = 'pipe',
  reshape = 'reshape',
  select = 'select',
  validate = 'validate',
}
